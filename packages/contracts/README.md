# @altera-ai/contracts

Shared schema and type contracts between the Altera AI backend (Python /
Pydantic) and the frontend (TypeScript).

**Status:** placeholder. See `src/index.ts` for the planned shape. The
full set of contracts lands alongside Phase 4 (Pydantic domain models)
and Phase 11 (export shapes).
