/**
 * @altera-ai/taxonomy
 *
 * Versioned canonical taxonomy. The taxonomy tree is published per-version
 * under `versions/<semver>/tree.yaml` and is loaded at runtime by the
 * backend rules engine.
 *
 * Status: placeholder. See docs/data/taxonomy.md for the canonical
 * specification.
 */

export const TAXONOMY_PACKAGE_VERSION = "0.0.1" as const;
export const CURRENT_TAXONOMY_VERSION = "0.0.1" as const;
