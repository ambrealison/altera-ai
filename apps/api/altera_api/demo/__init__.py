"""Demo-only feature package.

Everything in this package is gated behind an explicit, default-off
feature flag and is safe to delete after the demo. It must never change
production behaviour for normal uploads.
"""
