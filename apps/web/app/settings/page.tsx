import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Organisation, users, roles, and project defaults. Methodology and taxonomy versions are managed here, not in code."
      plannedPhase="Phase 12 — Frontend SaaS interface"
    />
  );
}
